import { motion } from 'framer-motion';
import styled from 'styled-components';

export const FullScreenDiv = styled(motion.div)`
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  bottom: 0;
`;
