{
  "name": "@miauflix/backend-client",
  "description": "Hono client to make calls to the backend API",
  "version": "1.0.0",
  "types": "./dist/index.d.ts",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp ../../dist/backend-client/index.d.ts dist/ && tsc --project tsconfig.json"
  },
  "dependencies": {
    "hono": "4.9.2"
  },
  "devDependencies": {},
  "peerDependencies": {
    "hono": "4.9.2"
  }
}
