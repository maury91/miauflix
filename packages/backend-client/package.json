{
  "name": "@miauflix/backend-client",
  "description": "Hono client to make calls to the backend API",
  "version": "1.0.0",
  "types": "./dist/index.d.ts",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "scripts": {
    "build": "npm run build:backend-client",
    "build:backend-client": "rm -rf dist && mkdir -p dist && cp ../../dist/backend-client/index.d.ts dist/ && tsc --project tsconfig.json"
  },
  "dependencies": {},
  "devDependencies": {
    "hono": "4.10.4"
  },
  "peerDependencies": {
    "hono": "4.10.4"
  }
}
